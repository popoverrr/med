# CONTENT-TODO — данные и материалы, которые нужны от заказчика

Всё, что ниже, сейчас представлено плейсхолдерами (`{{TOKEN}}` в конфигах → на сайте видны как пунктирные метки `TOKEN`) или медиа-заглушками `<MediaPlaceholder>`. Ничего из этого не выдумано и не заполнено «примерными» значениями (ТЗ §15).

## 1. Контакты — `src/content/config/contacts.json`

| Токен | Что нужно | Где используется |
|---|---|---|
| `CONTACT_PHONE` | Телефон в формате `+77XXXXXXXXX` (для `tel:` и JSON-LD) | шапка/футер, контакты, запись, JSON-LD `telephone` |
| `CONTACT_PHONE_DISPLAY` | Телефон как показывать: `+7 7XX XXX XX XX` | везде, где виден телефон |
| `CONTACT_WHATSAPP` | Номер WhatsApp `77XXXXXXXXX` | кнопки WhatsApp |
| `CONTACT_TELEGRAM` | Username или ссылка t.me | кнопки Telegram |
| `CONTACT_EMAIL` | E-mail для обращений (также — контакт для отзыва согласия на ПДн) | футер, контакты, политика |
| `CONTACT_INSTAGRAM` | Ссылка на Instagram (опционально) | футер |
| `HOURS_RU` / `HOURS_KK` | Часы работы текстом, RU и KZ («Пн–Сб 9:00–20:00») | контакты, футер, запись |
| `OPENING_HOURS_SCHEMA` | Часы в формате schema.org (`Mo-Sa 09:00-20:00`) | JSON-LD `openingHours` |
| `GEO_LAT` / `GEO_LNG` | Координаты входа (десятичные градусы) | JSON-LD `geo`, карта |
| `MAP_EMBED_URL` | Ссылка на встраиваемый виджет 2GIS (или Яндекс.Карт) | блок карты (грузится после согласия на cookie «Карта») |
| `MAP_LINK_URL` | Ссылка на карточку организации в 2GIS | кнопка «Открыть в 2GIS» |
| `ROUTE_DESCRIPTION` | Как добраться: ориентиры, вход, этаж (RU/KZ — в `ru.json`/`kk.json` → `pages.contacts.routeText`) | /contacts |
| `PARKING_INFO` | Парковка (RU/KZ — `pages.contacts.parkingText`) | /contacts |

## 2. Показатели воды — `src/content/config/water.json`

| Токен | Что нужно |
|---|---|
| `WATER_TDS_PPM` | Фактический замер TDS после ступени очистки (ppm), с датой замера |
| `WATER_PH` | Фактический pH после ионизации TYENT, с датой замера |

Температура 37 °C и «2 ступени» уже проставлены (следуют из ТЗ). Подтвердите также **технологию VOKO Aqualuxe** (что именно удаляет: механика/хлор/RO?) — текст ступени 1 сейчас сформулирован осторожно и обобщённо (`home.water.stages[0].text`). Если у оборудования есть паспортные данные или сертификаты — их можно добавить в раздел /water.

## 3. Цены — `src/content/config/prices.json`

| Токен | Что нужно |
|---|---|
| `PRICE_SINGLE` | Стоимость разовой процедуры (₸) |
| `PRICE_COURSE` | Стоимость курса (₸) |
| `COURSE_SESSIONS` | Количество процедур в курсе |
| `PRICE_COURSE_PLUS` | Стоимость курса с сопровождением (₸) |
| `COURSE_PLUS_SESSIONS` | Количество процедур в курсе с сопровождением |

Также: что именно входит в «сопровождение» (сейчас: «консультации специалиста между сеансами: питание, режим, контроль самочувствия») — подтвердить или переформулировать.

## 4. Домен, хостинг, формы

- **Домен**: сейчас `https://hydromed.kz` в `src/content/config/site.mjs` — подтвердить (влияет на canonical, sitemap, hreflang, OG).
- **www или без www**: `.htaccess` сейчас редиректит `www → без www`.
- **SMTP**: почтовый ящик отправителя на хостинге, хост/порт/логин/пароль → `public/api/config.php` (на сервере, не в репозитории).
- **Получатели заявок**: e-mail(ы).
- **Telegram**: токен бота и chat_id (если нужны уведомления в Telegram). Внимание: это трансграничная передача ПДн — уже описано в политике и согласии; если Telegram не нужен — убрать упоминания (см. LEGAL-REVIEW.md).
- **Хостинг-провайдер** (наименование) — для пункта 4.3 Политики конфиденциальности.

## 5. Медиа (все слоты — `src/content/media-manifest.json`, промпты — `IMAGE-PROMPTS.md`)

Файлы кладутся в `public/media/` под именами слотов (см. manifest), после чего плейсхолдеры автоматически заменяются на `<picture>`/`<video>` при сборке.

| Слот | Тип | Приоритет |
|---|---|---|
| `hero-water-loop` | видео 16:9 (webm+mp4, постер) | critical |
| `water-macro-loop` | видео 16:9 | high |
| `procedure-seq-001…090` | 90 кадров WebP 1280×720 → `public/media/sequence/` **и** 640×360 → `public/media/sequence/640/` (мобильные) | high |
| `procedure-poster` | изображение 16:9 | high |
| `specialist-video` | ✅ **заполнен** — видеообращение (главная, блок «Специалист»). Нужен текст речи для субтитров (WCAG 1.2.2) | critical |
| `specialist-portrait` | ✅ **заполнен** фото заказчика (/about) | high |
| `consultation-scene`, `consultation-scene-02` | ✅ **заполнены** фото заказчика (/procedure, пара 4:5) | medium |
| `device-hydrocolon` | ✅ **заполнен** фото заказчика у аппарата (/about, /procedure) | medium |
| `water-system-voko`, `water-system-tyent` | ✅ **заполнены** фото оборудования на студийном фоне | high |
| `room-interior-01`, `room-interior-02`, `disposable-kit-macro` | фото кабинета/оборудования | medium |
| `center-entrance` | фото входа в центр | low |

**Качество исходников.** Фото и видео пришли через WhatsApp и пережаты (фото 715×1280, видео 478×850, VOKO — 384×512). На сайте они смотрятся прилично, но на больших ретина-экранах мягковаты. Нужны **оригиналы** (из камеры/от фотографа, «файлом», не «фото» в мессенджере) — положить поверх тех же имён в `public/media/`. Исходники и параметры кадрирования — в `media-manifest.json` (description).
| `og-image-ru`, `og-image-kk` | 1200×630 — сейчас сгенерированы программно (`scripts/gen-icons.mjs`), можно заменить | critical |

Сейчас кадры image-sequence — программные плейсхолдеры с номером кадра (`scripts/gen-placeholders.mjs`), чтобы механика была видна.

## 6. Специалист

- Главный экран специалиста — **видеообращение** (`specialist-video`), на /about — фото. Оригиналы в полном качестве — см. §5.
- **Субтитры к видео**: пришлите текст того, что говорит Замира Ахметовна (RU и KZ) — сделаем `.vtt`-дорожки. Без них видео со звуком не проходит WCAG 1.2.2; сейчас оно по умолчанию без звука.
- Стаж: **20 лет** (с 2006 г.), образование: **врач** — указано со слов заказчика (`company.json` → `specialist.experience`, `specialist.education`). Нужны подтверждающие документы в центре (см. `LEGAL-REVIEW.md`). По желанию — специальность по диплому и вуз (добавим строкой в блок «Специалист»).
- Подтвердить, что **все процедуры проводит лично Замира Ахметовна** (так сказано в hero и FAQ). Если есть другие специалисты/администратор — добавить.

## 7. Тексты, требующие подтверждения заказчиком

- Формулировка hero: «Вода, подготовленная под вашу процедуру» (RU) / «Сіздің ем-шараңызға арнайы дайындалған су» (KZ).
- Шаги процедуры и их длительности (`home.how.steps`: 10–15 / 5–10 / 40–45 / 10 / 5 мин) — подтвердить фактическую практику центра.
- Рекомендации по подготовке и после процедуры (`pages.procedure.sections.prep/after`) — сверить со специалистом.
- Список показаний и противопоказаний (`home.indications`, `home.contra`) — **сверить со специалистом** (медицинская ответственность за перечень — на центре).
- FAQ (10 вопросов) — сверить ответы со специалистом.
- Казахская версия: полный перевод выполнен без носителя — нужна вычитка (ветки с `_needsReview` перечислены в отчёте `node scripts/check-i18n.mjs`).

## 8. Юридическое

См. `LEGAL-REVIEW.md` — все документы раздела /legal — проекты, требуют проверки юристом РК до публикации.

## 9. Аналитика

Не подключена. Если нужна (Яндекс.Метрика / GA4) — сообщите; подключение только после согласия в cookie-баннере (механизм уже есть: событие `hm:consent`, флаг `analytics`).
